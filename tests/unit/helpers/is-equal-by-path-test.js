
import { isEqualByPath } from 'car-fel/helpers/is-equal-by-path';
import { module, test } from 'qunit';

module('Unit | Helper | is equal by path');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isEqualByPath([42]);
  assert.ok(result);
});

