{{admin-nav}}

<div class="container"  background-image=url("/pix/samples/bg1.gif")>
	<font color="white">
		<h1>Nuevo Producto</h1>

	
	<div class="row">
		<div class="col-md-12">
			<form {{action 'createProduct' on="submit" }}>

				<div class="form-group">
					<label>Nombre</label>
					{{input value=model.product.name class="form-control" maxlength=30}}
				</div>

				<div class="form-group">
					<label>Descripción</label>
					{{input value=model.product.description class="form-control" }}
				</div>

				<div class="form-group">
					<label>Precio</label>
					{{input value=model.product.price class="form-control" maxlength=20}}
				</div>

				<div class="form-group">
				  <label for="sel1">Tipo de producto:</label>

				  	<select class="form-control" onchange={{action "selectTipo" value="target.value"}}>
 						{{#each model.type_promise as |tipo|}}
  					  		<option value={{tipo}} selected={{eq eltipo tipo}}>{{tipo.name}}</option>
 						{{/each}}
					</select>

{{!-- 				<select name="role" class="form-control" value="target.value"  >
 				{{#each model.product_promise as |tipo|}}
 					 <option value="{{tipo.id}}">{{tipo.id}} {{tipo.name}} </option>
 				{{/each}}
			</select> --}}
				</div>
				<button type="submit" class="btn btn-success">Crear</button>
			</form>
		</div>
	</div>
		</font>
</div>
